# Dynamic Incorrect Answer Injection: 2x max baseline CoT tokens
# For each question, injects "The answer must be X then! " repeated to match
# 2x the max token count from that question's baseline CoT samples.
name: "incorrect_dynamic_max_2x"

model:
  name: "Qwen/Qwen3-8B"

questions:
  source: "bbq"
  categories:
    - age
    - disability
    - gender
    - nationality
    - appearance
    - race
    - race_ses
    - race_gender
    - religion
    - ses
    - sexual_orientation
  n_per_category: 10
  seed: 42

generation:
  temperature: 0.6
  top_p: 0.95
  max_tokens: 3000  # Higher limit for 2x max tokens
  enable_thinking: true
  use_few_shot: true

override:
  token_to_match: "<think>"
  intercept_count: 1
  dynamic:
    mode: "incorrect_answer"
    metric: "max"
    multiplier: 2

samples: 10

